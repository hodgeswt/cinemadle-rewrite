{
    "NLog": {
        "autoReload": true,
        "throwConfigExceptions": true,
        "targets": {
            "logfile": {
                "type": "File",
                "fileName": "${specialfolder:folder=LocalApplicationData}/AppData/cinemadle.log",
                "layout": "${longdate} ${level} ${message} ${exception}"
            }
        },
        "rules": [  
            {
                "logger": "Cinemadle*",
                "minLevel": "Info",
                "writeTo": "logfile"
            }
        ]
    },
    "AllowedHosts": "*"
}